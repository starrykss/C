/*
#include <stdio.h>

int main(void)
{
	char days[365];
	int x, y;

	char mon = "MON";
	char tue = "TUE";
	char wed = "WED";
	char thu = "THU";
	char fri = "FRI";
	char sat = "SAT";
	char sun = "SUN";

	for (int i = 0; i < 365; i++) {

	}



	while (1) {
		scanf("%d %d", &x, &y);

		if ((x < 1) || (x > 12) || (y < 0) || (y > 31)) {
			return 0;
		}
		else {
			switch (x) {
			case 2:;
			case 1, 3, 5, 7, 8, 10, 12:;
			case 4, 6, 9, 11:;
			}
		}
	}
}
*/
