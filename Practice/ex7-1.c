// 2개의 함수로 만든 프로그램
// 함수 안에 사용하는 변수명은 다른 함수의 변수명과 같을 수 있음.
/*
#include <stdio.h>

int add(int x, int y);		

int main(void) {
	int a = 10, b = 20;
	int res;

	res = add(a, b);
	printf("result : %d\n", res);

	return 0;
}

int add(int x, int y) {
	int temp;

	temp = x + y;

	return temp;
}
*/