// char형 변수의 사용
// %c를 사용하면 문자를 출력하고 %d를 사용하면 아스키 코드값을 출력.

/*
#include <stdio.h>

int main(void) {
	char ch1 = 'A';		// 문자로 초기화		
	char ch2 = 65;			// 문자 'A'의 아스키 코드값으로 초기화

	printf("문자 %c의 아스키 코드값 : %d\n", ch1, ch2);				// %c에 char 값 대입 -> 문자 변환
	printf("아스키 코드값이 %d인 문자 : %c\n", ch1, ch2);			// %d에 char 값 대입 -> 아스키 코드값 변환

	return 0;
}
*/