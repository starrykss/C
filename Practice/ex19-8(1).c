// 두 정수의 평균을 구하는 프로그램
// 분할 컴파일은 하나의 프로그램을 여러 개의 파일로 나누어 작성하면 됨
/*
#include <stdio.h>

void input_data(int *, int *);
double average(int, int);
int main(void) {
	int a, b;
	double avg;

	input_data(&a, &b);
	avg = average(a, b);
	printf("%d와 %d의 평균 : %.1lf\n", a, b, avg);	

	return 0;
}
*/