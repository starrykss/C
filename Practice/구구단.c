/*
#include <stdio.h>

int main(void)
{
	int n;

	while (1) {
		scanf("%d", &n);
		if ((n < 1) || (n > 9)) {
			return 0;
		}
		else {
			for (int i = 1; i < 10; i++) {
				printf("%d * %d = %d \n", n, i, n*i);
			}
			break;
		}
	}
	return 0;
}
*/