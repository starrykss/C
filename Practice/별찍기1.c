/*
#include <stdio.h>

int main(void)
{
	int n;
	
	while (1) {
		scanf("%d", &n);
		if ((n < 1) || (n > 100)) {
			return 0;
		}
		else {
			for (int i = 0; i < n; i++) {
				for (int j = 0; j <= i; j++) {
					printf("*");
				} //end of for
				printf("\n");
			} // end of for
			break;
		}	// end of else
	} // end of while
	return 0;
}
*/