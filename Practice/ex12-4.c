// gets 함수로 한 줄의 문자열 입력
// gets 함수는 엔터 키만 쳐도 입력을 끝냄.
// 공백문자, 탭문자, 개행문자 모두 출력
/*
#include <stdio.h>

int main(void) {
	char str[80];

	printf("공백이 포함된 문자열 입력 : ");			
	gets(str);				// 배열명으로 주고 함수 호출
	printf("입력한 문자열은 %s입니다.\n", str);

	return 0;
}
*/